const { execute } = require("./help");

module.exports = {
    name: "reload",

    args: true,

    usage: "<Command name>",

    description: "Reloads a command",

    execute(message, arguments) {

    }
}